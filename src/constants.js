/**
 * Emitter event types.
 * (Just one right now).
 */
export const PROGRESS_UPDATE = 'progressUpdateEvent';

/**
 * Default values.
 */
export const DEFAULT_GROUP = 'defaultGroup';
export const DEFAULT_DELAY = 0;
